package main

func main() {
	// q := Queue{size: 2}
	// s := Stack{size: 4}

	// q.enqueue(1)
	// q.enqueue(13)
	// fmt.Println(q.values)
	// fmt.Println(q.peek())
	// q.dequeue()
	// fmt.Println(q.values)
	// q.dequeue()
	// fmt.Println(q.values)
	// q.dequeue()

	// s.peek()
	// s.push(5)
	// s.push(13)
	// fmt.Println(s.values)
	// fmt.Println("peek: ", s.peek())
	// s.pop()
	// fmt.Println(s.values)
	// s.pop()
	// fmt.Println(s.values)
	// s.pop()
}
